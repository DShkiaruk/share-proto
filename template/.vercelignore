# Local-mode files must not reach a Vercel deployment: a root server.js makes
# Vercel treat the project as a Node server and skip api/ + middleware.
# Anchored to the project root so prototype assets like public/data/ survive.
/server.js
/data/
